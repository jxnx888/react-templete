import React from 'react';
import './index.scss';

interface IProps {
  [PropName: string]: any
}

const Index = (props: IProps) => {
  const {} = props || {};
  return (
    <div className={'container'}>

    </div>
  );
};
export default Index;